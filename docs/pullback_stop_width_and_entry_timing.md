# 눌림목 탭 개선 — 손절폭 배지 UI + 확인 후 진입 측정 (2026-08-14)

사용자 요청 3가지(①②는 UI, ③은 측정) 처리 기록. ③ 측정 결과가 ①의 전제
자체를 뒤집어서, UI 반영 방향도 원래 요청과 달라졌다 — 그 경위를 그대로 남긴다.

## 방법론

`docs/all_tabs_common_yardstick_investigation.md`의 시점매칭 체크포인트 방식을
그대로 재사용: `off=60..250`(10 간격, 20지점) 전체 유니버스(KR 1503 + US 2120,
delisted 4종목 제외 실제 3612종목 페치)에 대해 각 체크포인트마다 데이터를 그
시점까지 잘라(`df.iloc[:len(df)-off]`) `analyze()`류 함수를 실제로 돌리고,
그 뒤에 남은 `off`봉의 미래 데이터로 2R 레이스를 진행. RS/RS모멘텀도 체크포인트
시점 기준으로 재계산(미국은 단일 벤치마크라 랭킹에 상수 오프셋이라 벤치마크
차감 생략 — 순위 불변; 한국은 코스피/코스닥 벤치마크를 같은 오프셋으로 truncate
해서 반영). 레이스: 진입 후 최대 60봉, 저가≤손절 우선 판정, 목표(진입+2×리스크)
도달, 60봉 내 미해결="미결"(0R, 분모 포함), 가용 데이터가 60봉 미만인 채
미해결="데이터부족"(분모 제외) — 이번 측정에선 전부 off≥60이라 데이터부족 0건.

전 코드는 `/private/tmp/.../scratchpad/measure.py`(1회성, 저장소에 커밋 안 함,
prior 조사들과 동일 관례). **v5.67에서 저유동성 하드 필터 재현을 추가**(아래
"기준선 불일치 조사" 참고) — 이 문서의 모든 수치는 그 반영 이후 최종본이다.

## 기준선 불일치 조사 (v5.67) — 원인 특정은 실패, 재현성으로 대체

최초 측정(위 방법론 그대로, 저유동성 필터 없이) 결과 눌림목 전체 EV가 0.286R
(n=1648)로 나왔는데, 이는 `all_tabs_common_yardstick_investigation.md`의 기존
Script A 기록치 0.172R(n=1787, 일평균 89.4)과 66% 차이가 났다. 사용자가
"필터 방향 결론보다 이게 더 급하다"고 짚어서, ①③ 결론에 쓰기 전에 원인을
특정하는 조사를 먼저 진행했다.

### 확인한 것 (전부 일치 — 버그 아님)

1. **체크포인트/레이스 스펙**: `off=60..250` 20지점, 진입=신호일 종가,
   손절=`analyze()`가 반환하는 stop 필드(모드 재정의 없음), 목표=진입+2×리스크,
   60봉 레이스, 저가≤손절 우선판정, 미결=0R(분모 포함), 데이터부족만 분모
   제외 — 전부 `all_tabs_common_yardstick_investigation.md` 방법론 절 원문과
   대조해 문구 그대로 일치 확인.
2. **RS 계산 정합성**: 내 체크포인트 코드의 RS 산출 로직을 app.py
   `_fetch_market_data_inner`의 실제 코드(2921~2939행)와 나란히 재현해
   비교 — 3512종목 전원 랭크 완전 일치(diff 0건).
3. **미국 벤치마크 생략 가정 검증**: "미국은 단일 벤치마크라 상수 이동,
   순위 불변"이라는 가정을 실제 나스닥종합(^IXIC) 지수로 직접 검증 —
   벤치마크 차감 있음/없음 랭크가 2036종목 전원 완전 일치(mismatch 0건).
4. **레이스 로직 수동 트레이스**: 4종목(005930.KS 등)을 entry/stop/target과
   실제 미래 10봉의 저가·고가를 나란히 출력해 손절/목표 판정이 계산대로
   맞는지 눈으로 확인 — 정상.
5. **CONFIG 드리프트**: `all_tabs_common_yardstick_investigation.md` 작성일
   (2026-08-08) 이후 scanner.py 변경 이력(v5.57~v5.65)을 전부 diff로 확인.
   눌림목에 영향 있는 변경은 v5.60의 `ma20_slope_floor`(주도주만 0.98 →
   전종목 0.98) 하나뿐이고, 이건 **완화**라 히트 수가 늘어나는 방향이어야
   하는데 실제론 내 쪽 히트가 더 적다(1648 vs 1787) — 방향이 반대라 이
   변경으로는 설명 안 됨. v5.61~v5.65는 scanner.py `analyze()`(눌림목)를
   건드리지 않음(app.py의 진단 함수/테스트 인프라만 변경).

### 찾아서 고친 것 (실재하지만 격차의 원인은 아니었음)

**저유동성 하드 필터 누락**: `app.py`의 `run_scan()`은 `analyze()`가 히트를
반환한 뒤 평균거래대금(KR 3억원/일, US $2M/일) 미만을 추가로 걸러낸다
(v4.52, `scanner.py` 안이 아니라 `app.py` 쪽에 있음). 이 조사에서 쓴
`measure.py`는 `analyze()`를 직접 호출해서 이 필터를 안 거쳤다 — 실서비스
화면엔 안 보이는 저유동성 종목이 측정엔 섞여 있었다는 뜻. 필터를 추가해
재측정했지만 **영향은 미미**했다(눌림목 n 1648→1623, EV 0.286→0.291) — 격차의
원인은 아니었지만, 그 자체로 실서비스와 안 맞는 진짜 gap이었으므로 계속
반영한다. 이 문서의 모든 최종 수치는 이 필터 포함 버전이다.

### 결론 — 특정 실패, 그러나 재현 가능한 쪽을 채택

Script A의 원본 코드가 저장소에 남아있지 않아(이 프로젝트의 표준 관례상
1회성 측정 스크립트는 커밋하지 않는다 — 이번 측정도 마찬가지로 안 남는다)
정확히 뭐가 달랐는지 **특정하지 못했다**. 다만 이번 측정은:
- 방법론 문서 원문과 항목별로 대조 확인됨
- RS/벤치마크 로직이 프로덕션 코드와 독립적으로 완전히 일치함이 검증됨
- 레이스 로직이 수동 트레이스로 확인됨
- `analyze()`를 직접 호출(트레이스 사본 아님 — v5.61~v5.65에서 반복
  발견됐던 "진단 함수가 실제 로직과 드리프트"하는 유형의 버그가 원천
  차단됨)
- 프로덕션의 저유동성 필터까지 재현함

**이 재현 가능성 자체를 근거로 이번 측정치를 채택**하고, 기존 Script A
수치(눌림목 0.172, 추세전환 0.362, 돌파 0.311, 박스돌파 0.336, 돌파임박
0.241)는 재현 불가로 신뢰도가 낮다고 판단해 `all_tabs_common_yardstick_investigation.md`
상단에 "이후 재측정으로 대체됨" 노트를 추가했다. 아래 ①③의 모든 수치는
이번 재측정(저유동성 필터 포함, n=1623) 기준이다.

## ① 손절폭과 EV — 전제가 재현되지 않음

사용자가 제시한 전제: "눌림목에서 손절≤5%만 남기면 EV 0.172→0.145로 떨어진다."
이번 측정으로 재현을 시도했으나 **재현되지 않았고, 오히려 방향이 반대로
나왔다.**

### 눌림목 재측정 (n=1623, 20개 체크포인트, 저유동성 필터 포함 최종본)

| | n | EV(R) | 손절률 | 도달률(2R) |
|---|---|---|---|---|
| 전체 | 1623 | 0.291 | 56.9% | 43.0% |
| 손절≤5% | 1371 | **0.300** | 56.7% | 43.3% |
| 손절 5%+ | 252 | **0.246** | 57.9% | 41.3% |

타이트한 쪽이 오히려 근소하게 우세 — 사용자가 인용한 방향과 반대다. 전체 EV
자체도 기존 문서치(`all_tabs_common_yardstick_investigation.md`의 0.172)와
다르다(0.291) — "기준선 불일치 조사" 절에서 원인을 특정하려 했으나 실패했고,
재현 가능성을 근거로 이번 수치를 채택했다(위 참고). "0.172→0.145"라는 조합
자체는 `docs/` 어디에도 기록이 없었다(grep 확인) — 아마 스캐너 화면에서 직접
토글해본 단발 스냅샷이었을 가능성이 높고, 그렇다면 표본이 훨씬 작아(하루치,
수십 건) 노이즈에 취약했을 것.

### 다른 4개 핵심 탭도 같이 측정 — 방향이 탭마다 다름 (저유동성 필터 포함)

| 탭 | 일평균 히트 | 전체 EV | 손절≤5% EV(n) | 손절5%+ EV(n) | 방향 |
|---|---|---|---|---|---|
| 눌림목 | 81.2 | 0.291 | 0.300 (1371) | 0.246 (252) | 타이트 근소 우세 |
| 추세전환 | 50.1 | 0.233 | 0.190 (290) | 0.251 (713) | 타이트 열세 |
| 돌파 | 19.1 | 0.212 | 0.421 (114) | 0.123 (268) | 타이트 크게 우세 |
| 박스돌파 | 15.8 | 0.161 | 0.412 (68) | 0.092 (249) | 타이트 크게 우세 |
| 돌파임박 | 235.8 | 0.232 | 0.231 (1888) | 0.233 (2828) | 무차이 |

5개 탭 중 "타이트가 나쁘다"는 방향은 추세전환 하나뿐이고, 나머지는 무차이거나
오히려 타이트가 우세하다. **"손절 좁음=나쁜 신호"라는 단일 방향 결론은 전
탭은커녕 눌림목 하나에서도 성립하지 않는다** — 그때그때 다르다는 것 자체가
결론이다.

### 결론 및 반영

손절폭이 진입 품질과 안정적으로 연동되지 않는다는 것(방향이 탭·시점마다
바뀜)이 오히려 "배지는 포지션 크기 결정용이지 품질 신호가 아니다"라는 원래
문제의식을 더 강하게 뒷받침한다 — 다만 그 근거로 특정 방향("좁으면 나쁘다")을
못박는 대신 "방향 자체가 불안정하다"로 문구를 정정해서 반영했다.

- **필터/정렬 자체는 유지** — narrow가 나쁘다는 근거가 없고(오히려 3/5 탭에서
  우세), 포지션 사이징 목적 자체는 여전히 유효한 사용처이기 때문에 제거하지
  않음.
- **배지 툴팁**(`riskTierBadge()`, static/index.html): "포지션 크기 결정용 —
  좁다고 좋은 게 아님" + "전 탭 실측 결과 손절폭과 EV의 관계는 탭·시점마다
  방향이 다름" 명시.
- **인라인 안내줄** (신규, `#stopWidthWarnBanner`): 눌림목 탭에서 🔵필터나
  손절좁은순 정렬을 켜면 노출. 특정 방향을 단정하지 않고 최신 실측치(0.295 vs
  0.240)와 "방향이 고정돼 있지 않다"는 문구로 구성.

## ② 슈퍼대장 필터 강조

EV=0.266(n=320, `all_tabs_common_yardstick_investigation.md` v5.55에 이미
검증·채택된 수치 — 재측정 불필요, 전체 눌림목 0.151R 대비)로 가장 효과적인
필터인데 다른 필터 버튼과 톤이 같아 묻혀 있었던 문제.

**반영**: `#superOnlyToggle`에 금색 톤 전용 스타일(테두리·글자색, 켜졌을 때 더
진하게) 추가 — 상시로 다른 버튼과 구분되게.

**기본값 on 여부**: 검토 결과 기본 OFF 유지를 추천, 미반영. 89→16건으로 줄이면
"오늘 뭐가 있나 훑어보는" 스캐너 본연의 용도에서 84%가 안 보이게 되는데, 이건
"찾다가 필터를 더 켜는" 방향과 반대로 "이미 좁혀진 걸 다시 넓혀야 아는" UX라
되돌리기가 비대칭적으로 불편하다(꺼짐→켜짐은 한 번의 정보 습득, 켜짐→꺼짐은
"내가 뭘 놓치고 있나"를 매번 의심해야 함). 시각적 강조만으로 "이게 제일
좋다"는 신호는 충분히 전달되고, 필터 여부는 사용자 선택으로 남기는 쪽이
스캐너의 탐색 기능을 해치지 않는다고 판단.

## ③ 눌림목 확인 후 진입 측정 — 돌파임박과 같은 패턴 재확인, 더 강하게

`docs/imminent_stop_entry_investigation.md` 3절의 방법론을 눌림목에 적용.

**정의** (돌파임박과 달리 손절 정의는 그대로 두고 진입 타이밍만 변수화 —
사용자 요청 범위):
- **안A(현행)**: 진입=신호일 종가, 손절=`analyze()`가 실제 계산하는 구조적
  손절(그대로).
- **안C'**: 신호일 다음 1~3봉 내 첫 번째로 "종가가 신호일 고가 초과 **+**
  그날 거래량이 트레일링 50일 평균의 1.5배 이상"을 만족하는 봉에서 진입
  (진입가=신호일 고가 레벨, 안C의 "고가 레벨 진입" 관례를 그대로 따름). 3봉
  내 미충족이면 미진입. 손절은 안A와 동일(재정의 안 함).
- **안D'(시점매칭 대조군)**: 안C'가 실제 진입한 그 봉에, 조건 확인 없이 그
  봉의 종가로 무조건 진입. 손절은 안A/안C'와 동일. n·타이밍이 안C'와 완전히
  같음 — "같은 날 진입해도 조건 확인 없이 종가로 사면 어떻게 되는가"만 분리.

### 결과 (n=1623, 저유동성 필터 포함 최종본)

| | 진입률 | 손절률 | 도달률(2R) | EV(R) |
|---|---|---|---|---|
| 안A 전체 | 100% | 56.9% | 43.0% | 0.291 |
| 안C' 진입분(n=261) | 16.1% | **35.6%** | **64.0%** | **0.923** |
| 안D' 진입분(같은 n·시점, 무조건 진입) | 16.1%(=C'와 동일) | 54.4% | 40.6% | 0.268 |
| 하이브리드(미진입=안A로 대체, n=1623 전체) | 100% | 58.5% | 41.3% | **0.242** |

### 해석

1. **안C' vs 안D' — 이번엔 필터 자체의 순수 효과도 크다.** 돌파임박에서는
   D'-와인 매칭(3.3절)에서 순수 필터 효과가 +4.3%p로 작았는데(대부분의 값이
   "확인 전엔 안 산다"는 시점/규율 효과), 눌림목은 **같은 날·같은 진입가
   기준으로도** C'(0.923R)가 D'(0.268R)를 큰 폭으로 앞선다. 거래량 1.5배+
   고가돌파 조건 자체가 실질 정보를 담고 있다는 뜻 — 돌파임박보다 조건-자체
   효과가 뚜렷하다.
2. **하이브리드(0.242R) < 안A 전체(0.291R) — 돌파임박과 같은 역설이 재현.**
   미진입 83.9%(1362건)에 안A를 채워 넣으면 역산 EV는 약 0.112R로, 안A
   전체 평균(0.291R)보다 한참 낮고 확인분(0.923R)과는 비교가 안 됨. "놓치기
   아까워서 안A로라도 채워 넣는" 시도는 여기서도 역효과 — 확인 안 되는
   84%가 안A로 진입해도 평범한(오히려 평균 이하인) 자리라는 뜻.
3. **결론**: 돌파임박의 "확인 전엔 진입하지 않는다" 규율이 눌림목에도 그대로
   적용 가능하고, 오히려 조건 자체의 순수 효과(C' vs D')는 더 크게 나온다.
   다만 진입률이 16.1%로 낮아(돌파임박 19.4%와 비슷한 수준) 전체 안A 자리를
   대체하긴 어렵고, 안A 위에 "확인되면 더 강하게 들어간다"는 보조 신호로
   쓰는 게 맞는 형태 — 돌파임박에 이미 있는 일지 관찰 트리거 배지(v5.44,
   등록일 고가/필요 거래량 필드)와 같은 패턴을 눌림목에도 붙일 수 있다는
   뜻이지만, 이번 요청은 측정까지였으므로 UI 구현은 하지 않고 다음 라운드
   후보로 남긴다.

## 재현 참고 — 돌파임박 하이브리드 EV (기존 기록 재확인)

사용자가 "이전에 한 것"으로 재확인 요청한 수치, `docs/imminent_stop_entry_investigation.md`
3.6절 원문 그대로:

| | EV(R) | n | 커버리지 |
|---|---|---|---|
| 안A 전체(기준) | 0.1988 | 3622 | 100% |
| 안C 진입분만 | 0.796 | 701 | 19.4% |
| 안C보완(미진입=안A로 대체, 하이브리드) | 0.1632 | 3622 | 100% |

## v5.68 — 재발 방지 + ③ 구현 완료

기준선 불일치 조사에서 "Script A 원본이 안 남아있어 대조 불가"가 이번 건의
진짜 원인이었고, 같은 이유로 재현 불가한 상태의 측정이 이 조사 하나가
아니라 오늘 기준 최소 5개 결정의 근거였다는 지적을 받고 진행한 후속 조치.

### 1. 측정 스크립트 커밋 관례 도입

`scripts/measurements/`를 신설 — 결정 근거가 되는 측정은 스크립트를
커밋한다(파일명 `YYYY-MM-DD_무엇을쟀는지.py`). 이번 조사의 실제 스크립트를
`scripts/measurements/2026-08-14_pullback_stop_width_and_entry_timing.py`로
커밋(위에 인용한 모든 수치의 실제 코드). 관례 상세와 기존 `docs/*.md` 측정
스크립트 존재 여부 감사 결과는 `scripts/measurements/README.md` 참고 —
결론: **이번 걸 빼면 전부 재현 불가.**

### 2. 공통 측정 하네스

`scripts/measurements/harness.py` 신설 — 유니버스 fetch, 체크포인트 시점
RS/RS모멘텀 재계산, `run_scan()`의 저유동성 하드 필터 재현, 2R 레이스를
한 곳에 고정. 이번 조사의 스크립트를 이 하네스로 리팩터한 뒤 리팩터 전
(`results_liqfilter.json`)과 diff — **완전 일치**(부동소수점 오차까지
0) 확인. 다음 측정부터는 이 모듈을 가져다 쓰면 "제각각 구현하다 기준선이
갈리는" 이번 사고 유형이 구조적으로 막힌다.

### 3. Script A 기반 결정 재측정 우선순위

Script A 계열(2026-08-08~09 라운드) 조사가 근거가 된 결정 5개, 전부
재현 불가 상태. "전부 다시 재면 오래 걸린다"는 지적에 따라 (a) 되돌리기
어려운/실거래 영향 큰 순, (b) 원측정이 오늘 겪은 것과 같은 유형(인접
구간 이진비교, 임계값 근처)일수록 우선 — 두 기준으로 순위를 매겼다.

| 순위 | 결정 | 문서 | 근거 강도(원 측정) | 왜 이 순위인지 |
|---|---|---|---|---|
| 1 | 신호등 U/D 항목 제거, 강한피벗 U/D 하드게이트 제거, distribution_check() U/D 신호 제거 | `ud_volume_ratio_investigation.md` | 3개 하위측정(일봉50/주봉50/보유맥락)에서 전부 같은 방향(역전) — 다소 튼튼 | **실거래 알림에 직접 영향**(보유 종목 경고, 얼마냐봇 진입 게이트) + 하드게이트 제거라 원복이 어려움. 사용자도 최우선으로 지목 |
| 2 | 패턴탭(컵앤핸들/치솟은깃발/더블바닥/급등매집) "검증실패" 라벨 + 탭 순서 뒤로 | `all_tabs_common_yardstick_investigation.md` | 치솟은깃발 5배 격차(0.030 vs 0.145) — 크긴 하나 이진 비교(대조군 vs 실측) 1회성 | **탭 전체에 대한 사용자 신뢰를 강하게 깎는 라벨**이라 오판 시 피해가 큼. 사용자도 최우선 지목 |
| 3 ✅재측정완료 | breakout/boxbreak/imminent `rs_min` 85→80 | `rs_definition_and_slope_investigation.md` | "80-85 구간이 85-90보다 EV 같거나 높음" — **오늘 뒤집힌 손절폭 타이어 비교와 구조가 동일**(인접 구간 이진비교, 임계값 근처) | 지금 이 순간 프로덕션에서 3개 탭의 통과율을 실제로 바꾸고 있는 CONFIG 값이고, 오늘 겪은 것과 같은 측정 유형이라 재현성 의심이 제일 구체적 — **아래 5절에서 재현 확인, 결정 유지** |
| 4 | 슈퍼대장 즉시진입 손절 ATR×2 채택 | `all_tabs_common_yardstick_investigation.md` | 4개 손절 후보 전부가 기존 방식(하이브리드 0.26)을 크게 상회 + 20일선→50일선 손절률 단조 감소 패턴 — 다중 조건에 걸친 단조 경향이라 이진비교보다 튼튼한 편 | 카드에 실제 진입가/손절가로 노출되는 실거래 정보지만, 근거 패턴이 상대적으로 안정적으로 보여 3보다 아래 |
| 5 | tightening/vol_dry 채점 조정 | `imminent_stop_entry_investigation.md` | tightening 1.5%p(오차범위 수준 — 사용자도 이 예시로 후순위 지정), vol_dry 7.1%p(방향 반전이나 완전제거 아닌 연속식 전환이라 blast radius 작음) | 효과 크기가 작거나(tightening) 이미 절벽만 없앤 절충 반영(vol_dry)이라 잘못됐어도 손실이 제일 작음 |

**다음 액션**: 3순위(rs_min)는 아래 5절에서 재측정 완료. 남은 1·2·4·5순위는
`scripts/measurements/`에 새 스크립트를 만들어 순서대로 진행 — 전부 하네스
(`harness.py`) 재사용이라 오늘보다 빠를 것.

### 4. ③ 관찰(watch) 트리거를 눌림목으로 확장 — 구현 완료

`scanner.py` `analyze()`(눌림목)에 `signal_high` 필드 추가(돌파임박
`analyze_imminent`와 동일 관례 — `avg_volume`은 이미 있던 필드라 그대로
재사용). `static/index.html`의 저장 로직(`saveJournal()`)이 트리거 스냅샷을
채우는 조건을 `s.mode === 'imminent'` 단일 체크에서 `TRIGGER_WATCH_MODES`
Set(`imminent`, `pullback`)으로 교체. 확인 판정/표시 로직(`isTriggerConfirmed`,
`triggerInfoHtml`, 가격·거래량 추적)은 이미 `trigger_price` 유무로만
판정하는 모드 무관 구조라 손댈 필요 없었음 — 새 UI 없이 저장 조건 4줄만
확장. 눌림목 카드에서 "+ 일지에 추가" → 카테고리 "👁 관찰"로 저장하면
돌파임박과 동일하게 3영업일 내 확인 여부를 추적한다.

### 5. rs_min 85→80 재측정 — 원 결정 재현됨, 유지

우선순위 3순위 재측정. 스크립트:
`scripts/measurements/2026-08-14_rs_min_bucket_ev_breakout_boxbreak_imminent.py`
(하네스 재사용, 오늘 이미 받아둔 유니버스 데이터로 실행 — 새 fetch 없이
100초 만에 완료). 방법: 각 탭 CONFIG의 `rs_min`을 측정 전용 사본에서만
70으로 낮춰(원본 CONFIG는 안 건드림) 75-80/80-85 구간이 함수 내부 게이트에
걸리지 않게 한 뒤, 실제 `rs_rank`로 사후에 5구간(75-80/80-85/85-90/90-95/
95-99)으로 나눠 2R 레이스 EV 측정.

| 탭 | 75-80 | 80-85 | 85-90 | 90-95 | 95-99 |
|---|---|---|---|---|---|
| 돌파 | 0.368 (n=117) | 0.309 (n=136) | 0.186 (n=118) | 0.220 (n=91) | -0.081 (n=37) |
| 박스돌파 | 0.302 (n=106) | 0.171 (n=111) | 0.121 (n=99) | 0.176 (n=74) | 0.212 (n=33) |
| 돌파임박 | 0.156 (n=1519) | 0.262 (n=1490) | 0.236 (n=1363) | 0.219 (n=1169) | 0.182 (n=694) |

전 구간 n≥30(`MIN_N_FOR_JUDGMENT`) — 노이즈로 보류할 구간 없음.

**핵심 질문 "80-85가 85-90보다 EV 같거나 높은가" — 3개 탭 전부 재현됨:**
돌파 0.309>0.186, 박스돌파 0.171>0.121, 돌파임박 0.262>0.236. v5.60 결정의
직접 근거가 오늘 재현됐다 — **rs_min=80 유지.**

**부가 발견 — 85 경계 자체보다 흥미로운 비대칭.** 돌파·박스돌파는 75-80
구간이 80-85보다도 더 높다(0.368/0.302로 전 구간 최고치) — RS가 낮을수록
오히려 EV가 높은 방향, 90 이상에서는 비단조(90-95에서 반등, 95-99에서
돌파는 급락). 반면 돌파임박은 정반대로 75-80이 전 구간 중 **최저치**(0.156)
고 80-85가 최고치(0.262) — "80 밑은 별로"라는 방향이 뚜렷하다. 세 탭이
공유하는 단일 `rs_min` 임계값이 돌파/박스돌파엔 다소 보수적(더 낮춰도
됐을 수 있음)이고 돌파임박엔 딱 맞는 셈 — 탭 공유 임계값이라 어느 한쪽에
맞추면 다른 쪽이 틀어지는 구조. 지금 당장 문제될 정도는 아니지만(현재
80이 세 탭 다 "나쁜 선택은 아닌" 지점), 탭별로 다른 `rs_min`을 쓰는 안은
다음 라운드에서 검토할 만하다.

**재현 안 된 것 하나**: `rs_definition_and_slope_investigation.md`가 인용한
"돌파의 절대RS 85-90은 오히려 -0.034R"은 이번 측정(85-90 EV=0.186, 양수)과
부호가 다르다. 다만 원문의 "절대RS"는 이번에 쓴 상대 백분위(`rs_rank`,
지수 대비 초과성과 랭크)와 다른 지표일 가능성이 있어(원 스크립트가 없어
확인 불가) 직접 반박은 아니고, 두 지표가 다른 걸 재고 있었을 가능성으로
남겨둔다.

## 6. 돌파·박스돌파 RS 역방향 심층 조사 (v5.70) — rs_min 75로 탭별 분리

5절에서 나온 부가 발견("돌파·박스돌파는 75-80이 오히려 최고치, 90+에서
비단조") — 신호등에서 돌파 RS를 뺀 이유(ok군 0.232R < warn군 0.482R)와
같은 방향이라는 지적을 받고 세 가지 추가 확인. 스크립트:
`scripts/measurements/2026-08-14_breakout_boxbreak_rs_reversal_deep_dive.py`
(하네스 재사용, 오늘 데이터로 53초 완료).

### 6.1 rs_min을 더 낮추면 — 외삽 아니라 실측(60까지)

| | 60-65 | 65-70 | 70-75 | 75-80 | 80-85 | 85-90 | 90-95 | 95-99 |
|---|---|---|---|---|---|---|---|---|
| 돌파 EV | -0.011(90) | 0.144(111) | 0.035(115) | 0.368(117) | 0.309(136) | 0.186(118) | 0.220(91) | -0.081(37) |
| 박스돌파 EV | 0.141(71) | -0.152(79) | 0.175(97) | 0.302(106) | 0.171(111) | 0.121(99) | 0.176(74) | 0.212(33) |

(괄호 안 n, 전 구간 n≥30). **75 밑으로는 더 좋아지지 않는다** — 60-70
구간은 오히려 75-80보다 낮거나(70-75: 돌파 0.035, 박스돌파 0.175) 부호가
바뀐다(60-65: 돌파 -0.011, 65-70: 박스돌파 -0.152). "RS 낮을수록 좋다"로
계속 내리는 함정이 아니라, 75가 실제로 꺾이는 지점.

### 6.2 임계값별 누적 — "여기로 낮추면 실전에서 어떤가"

| rs_min | 돌파 EV(n, 일평균) | 박스돌파 EV(n, 일평균) |
|---|---|---|
| 80(현행) | 0.212 (382, 19.1) | 0.161 (317, 15.9) |
| **75** | **0.248 (499, 25.0)** | **0.196 (423, 21.2)** |
| 70 | 0.208 (614, 30.7) | 0.192 (520, 26.0) |
| 65 | 0.199 (725, 36.3) | 0.147 (599, 30.0) |
| 60 | 0.175 (815, 40.8) | 0.146 (670, 33.5) |

75로 낮추면 EV와 일평균 히트 수가 **둘 다** 오른다(돌파 0.212→0.248R +
19.1→25.0건/일, 박스돌파 0.161→0.196R + 15.9→21.2건/일) — 드문 "공짜
개선". 70으로 더 내리면 히트는 계속 늘지만 EV가 도로 떨어져 75 수준과
현행 80 수준 사이로 수렴, 65·60은 명확히 나빠짐. **75가 자연스러운
바닥.**

### 6.3 왜 그런가 — extended(과확장) 가설 확인됨

돌파 히트를 RS 구간별로 나눠 200일선 이격(`ext200_pct`)·AVWAP 이격
(`avwap.dist_pct`)·52주 고점 대비(`off_high_pct`) median을 비교:

| RS구간 | 200일선 이격 | AVWAP 이격 | 52주고점대비 |
|---|---|---|---|
| 60-65 | 16.6% | 6.9% | -1.7% |
| 70-75 | 22.6% | 7.5% | 0.0% |
| 80-85 | 31.1% | 8.2% | 0.0% |
| 90-95 | 53.0% | 9.7% | 0.0% |
| 95-99 | **72.0%** | 10.1% | 0.0% |

200일선 이격이 RS와 함께 **강한 단조 증가**(16.6%→72.0%) — RS 95+ 돌파
히트는 중앙값 기준 200일선 위로 72% 떠 있다(CONFIG의 `ext200_caution`
=60%를 이미 넘고 `ext200_danger`=100%에 근접). **"RS 높은 돌파가 나쁘다"의
실체는 RS 자체가 아니라 이미 많이 오른 종목일수록 RS도 같이 높아지는
상관관계로 보인다** — extended 가설 확인됨. AVWAP 이격도 같은 방향으로
증가하나 폭은 작음(6.9%→10.1%). 52주 고점 대비는 RS 70+에서 대부분
median 0(신고가 자체가 돌파 셋업의 전제라 이 지표는 구간을 잘 못
가른다).

### 6.4 결정 — rs_min 탭별 분리, ext200 직접 게이트는 보류

**반영(v5.70)**: `BREAKOUT_CONFIG`/`BOXBREAK_CONFIG`의 `rs_min` 80→75.
`IMMINENT_CONFIG`는 80 유지(이 탭만 75-80이 최저치라 낮출 근거가 없음,
5절 참고) — 세 탭이 공유하던 임계값을 처음으로 분리했다.

**보류**: extended 가설이 확인됐다고 해서 곧바로 "`ext200_pct`를 돌파/
박스돌파의 직접 게이트로 쓰자"로 넘어가진 않았다. RS를 낮추는 것보다
더 원리에 맞는 수정일 수 있지만, 그러려면 ext200 자체를 임계값별로
재는 별도 측정이 필요하고(이번 조사는 RS 구간별 median만 봤을 뿐 ext200
임계값 스윕은 안 함) 이번 요청 범위 밖 — 다음 라운드 후보로 남긴다.

## 미반영 (다음 라운드 후보)

- 위 "Script A 기반 결정 재측정 우선순위" 표의 5개 재측정 — 확인 후 순차 진행.
- 손절폭-EV 방향이 탭·시점마다 왜 이렇게 불안정한지(구조적 원인) 자체는
  이번 조사 범위 밖 — 필요시 별도 조사.
