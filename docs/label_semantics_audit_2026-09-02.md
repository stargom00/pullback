# 라벨-의미 불일치 전수 조사 (2026-09-02)

> **진행 상태**: 조사(1·2·3·4번) 전부 완료. 높음 심각도 6건 중
> **5건은 v5.152/v5.153에서 처리 완료**(CLAUDE.md rs_min 정정, 포지션
> RSI<50 배지 제거, 종가베팅 수치 6곳 단일소스화+GUIDE.md 직접갱신).
> **1건(vol_high→atr_pct_high 개명)도 v5.154에서 처리 완료**(4순위 —
> 파급범위 조사 후 승인받아 6곳 한 커밋으로 진행, 아래 "4순위 파급범위
> 조사" 절). **높음 심각도 6건 전부 처리 완료.** 중간·낮음 심각도는
> 전부 미처리, 목록만 유지(사용자 지시로 이번 라운드 대상 아님).

## 배경

같은 유형의 결함이 2026-09-02 하루 동안 5건 발견됨(turnover_rank/vok/
streak_days/GUIDE RSI/GUIDE A~D등급) — 코드는 정상 작동하지만 이름·
설명이 실제 의미와 어긋나 테스트로 못 잡히고 방치되는 클래스. 남은
것들을 체계적으로 찾는 게 목적.

## 심각도 기준

- **높음**: 실전 진입 판단에 직접 쓰이는 것(카드 배지, 등급, 진입신호등)
- **중간**: 해석을 오도하지만 진입 판단엔 간접적(주석, 진단정보, 통계표시)
- **낮음**: 내부 변수명 등 사용자에게 안 보이는 것

---

## 1. static/index.html 표시 라벨 (1차 패스)

| 위치 | 표시/이름 | 실제 의미 | 불일치 여부 | 심각도 | 근거 |
|---|---|---|---|---|---|
| static/index.html:3661 (포지션 탭 카드) | `RSI<50` 배지, 툴팁 "되돌림 계열 KR 조건에서 사후 EV가 낮게 나온 구간(측정 근거 있음)" | GUIDE.md 변경이력상 KR RSI 기반 규칙이 전부 철회됨. 특히 "RSI<50 회피"는 90개 체크포인트 재검증에서 부호까지 역전(-0.5~-0.65R→+0.063R)해 배지 자체를 백엔드에서 제거(v5.135, GUIDE.md:296-297) | **있음** — 프론트 배지가 이미 죽은 결론을 "측정 근거 있음"이라 단정. 대응 백엔드 로직 없음(순수 프론트 하드코딩, v5.135 배지 제거 시 이 사본을 안 지운 것으로 추정) | **높음** — 포지션 탭은 실보유 종목 화면, 매도/불안 판단에 영향 가능 | GUIDE.md:296-297(철회 이력), static/index.html:3661 |
| static/index.html:1196 (`entrySignal` 로컬 주석) | "탭별 측정 대상이 아니었던 항목 — 모든 탭에서 그대로 판정 반영" | `vol_high`/`atr_tight`는 `analyze()`(눌림목)만 채우고 breakout/imminent/pattern 히트엔 필드 자체가 없어 실제로는 눌림목 탭에서만 발동 | **있음(주석 vs 동작)** | **중간** — 사용자 비노출이지만 유지보수 오신 소지, 실제 신호등 판정에 영향 | static/index.html:1196 vs CLAUDE.md "알려진 설계 갭"(vol_high/atr_tight 항목) |
| static/index.html:1211 (`entrySignal` flags 집계) | "판정에 반영된(active) 항목만 집계" | 검증 결과 `c[3]`(탭별 active 여부) 필터가 정확히 구현돼 있어 **일치** | 없음(오탐 배제 기록용) | - | - |
| static/index.html:1914 / scanner.py:4339,4406 (stage2 tier) | "A티어: 52주 고점 -10% 이내" / "B티어: -10%~-25%" | `tier_a_mult=0.90`/`high52_mult=0.75` 정확히 일치 | 없음(검증 완료, 정상) | - | scanner.py:4339,4406 |

**1차 패스 커버리지**: entrySignal/entrySigMini/quietAccumBadge/
accumTooltip/stage2 tier/RS 근사 로직 등 카드 전역 배지 위주 확인
(title 속성 약 150곳 스캔).

### 2차 패스 — 종가베팅/재점화/돈의흐름/테마맵/저널·포지션 통계/오늘의 결정

| 위치 | 표시/이름 | 실제 의미 | 불일치 여부 | 심각도 | 근거 |
|---|---|---|---|---|---|
| static/index.html:1398 | 종가베팅 카드 툴팁 `"...base 대비 z=4.28"` | 같은 카드에 나란히 표시되는 `s.backtest_note`(app.py `JONGGA_BACKTEST_NOTE`, **이번 세션 v5.149에서 z=3.54로 갱신**)와 정면 충돌 — 같은 카드가 z값을 두 개 동시에 주장 | **있음(직접 검증 완료)** | **높음** — 진입판단 카드에 붙은 구체적 통계치, v5.149 작업 중 놓친 하드코딩 사본(내가 이번 세션에 만든 결함) | app.py `JONGGA_BACKTEST_NOTE`(v5.149) vs static/index.html:1398 |
| static/index.html:1425, 1430 | 종가베팅 포워드 성적 하단 `"백테스트 +1.22%"` | 신규 수치는 +0.80%(n=292, v5.149) — +1.22%/n=276은 철회된 구 유니버스 수치 | **있음(직접 검증 완료)** | **높음** — 실전 성과를 비교하는 기준선 자체가 낡음 | 위와 동일 |
| static/index.html:2200 | 종가베팅 탭 설명 `"...채택(z=4.28, docs/...)"` | 동일 | **있음(직접 검증 완료)** | **중간** — 탭 설명, 카드보다는 덜 두드러짐 | 위와 동일 |
| static/index.html:1404, 3326(코드 주석만) | `+1.22%,n=276,z=4.28` | 동일(주석) | **있음** | **낮음** — 사용자 비노출, 유지보수 혼동만 | 위와 동일 |
| static/index.html:1549 (재점화 "대기 N개" 헤더) | "대기"라는 제목 | `_reignition_watchlist_view()`(app.py:8779~) 자체 docstring: "status=watching/confirmed만 노출" — 헤더 카운트에 이미 확인진입(confirmed)된 항목까지 포함됨(목록 안에선 🔁+"확인진입"으로 구분 표시되긴 함) | **있음(제목이 구성을 과대표현)** | **중간** — 오판을 유발하진 않지만 "대기 5개"만 보고 실제 액션 필요 건수를 오인할 수 있음 | app.py:8779-8794, static/index.html:1504,1549 |
| static/index.html:5161 vs 5218-5225 (저널 "승률" 두 곳) | 두 통계 패널이 동일하게 "승률" 라벨 사용 | 5161행: `result_r>0` 기준(R부호). 5218행: `pnlPct>0` 기준(원시 손익% 부호) — 코드 주석에 "손절 데이터 없는 종료 건도 승/패에 잡히게" 의도적으로 다르게 설계했다고 명시돼 있음(의도적 설계, 버그 아님) | **있음(같은 라벨, 다른 정의 — 의도적이나 라벨이 구분 안 됨)** | **중간** — 수수료/슬리피지로 R부호와 원시%부호가 갈리는 경계 사례에서 두 패널이 다른 승률을 보여줄 수 있음, 사용자는 같은 지표로 오인하기 쉬움 | static/index.html:5161, 5216-5225(설계 의도 주석) |
| theme_lifecycle.py `turnover_share_declining_3d`("점유율 3일 연속 하락") | streak_days→streak_periods(일→주) 개명 건과 같은 유형 의심해 점검 | 확인 결과 **문제없음** — `market_turnover.index`(실제 거래일 날짜) 기반이라 money_flow.py의 주간 전환과 무관한 별도 일 단위 로직 | 없음(검증 완료, 오탐 배제 기록) | - | theme_lifecycle.py:100-148,381-384 |
| money_flow.py "확산(본격)" 단계 라벨 | 대장주 상승+2·3등주 중 1개 이상 상승 | 코드(202-204행)와 문서(30행) 정확히 일치 | 없음(검증 완료, 정상) | - | money_flow.py:30,202-204 |

**2차 패스에서 추가 확인, 문제없음**: 테마맵(theme_map) 탭 표시,
캘린더 "오늘의 결정" 🔴/🟡/⚪ 티어 라벨(즉시행동 항목은 전부 검증된
규칙을 실제로 충족한 것만 포함 — 이번 세션에 직접 구현하며 확인한
내용과 일치), `/guide` 페이지(정적 마크다운 그대로 렌더링, 동적 값
주입 없음 — 2번 조사 대상).

**1·2차 패스 종합 커버리지 완료**: 카드 배지 전역 + 종가베팅/재점화/
돈의흐름/테마맵/저널·포지션 통계/오늘의 결정. 1번 조사 사실상 완료로
간주하고 2번(GUIDE.md)으로 진행.

---

## 2. GUIDE.md 권장·금지 문구 대조

| 위치 | 표시/문구 | 실제 근거 상태 | 불일치 여부 | 심각도 | 근거 |
|---|---|---|---|---|---|
| GUIDE.md:324(6.5장 종가베팅 "근거") | "채택... n=276, 비용차감후 평균 익일갭 +1.22%, base 대비 z=4.28" | `docs/kr_jongga_betting_backtest.md`는 2026-09-01 재검증(진짜 거래대금 유니버스)으로 **z=3.54, n=292, +0.80%**가 현재 유효 수치(app.py `JONGGA_BACKTEST_NOTE`도 v5.149에서 이걸로 갱신됨). **직접 검증**: 바로 위 306-310행은 이번 세션 v5.149에서 v2 유니버스 반영을 설명하도록 갱신됐는데, 바로 아래 "근거" 문단만 옛 수치 그대로 — 같은 섹션 안에서 자기모순 | **있음(직접 검증 완료, 내가 v5.149에서 낸 결함)** | **높음** — 사용자가 탭 신뢰도를 판단하는 근거 문단 자체 | GUIDE.md:306-310(신) vs 324(구) — static/index.html에서 발견한 것과 동일 클래스 |
| GUIDE.md:144-197(👑 슈퍼대장 활용법, EV 0.266/0.172/0.641) | "실측 기반... 최강(EV 0.266 vs 무필터 0.172)", "ATR×2 즉시진입 EV 가장 높음(0.641)" | 근거`docs/all_tabs_common_yardstick_investigation.md`가 `scripts/measurements/README.md` 자체 감사표(이미 이 세션 초반에 확인한 표)에서 **"❌ 전부 재현 불가"**로 명시됨 — 방향성은 이후 부분 보강(KR역전·US시간분할 약화)됐지만 0.266/0.172/0.641 원수치 자체의 재현 스크립트는 없음 | **있음(추적 불가 상태 미표시)** | **중간** | scripts/measurements/README.md:111 |
| GUIDE.md:177("대장후보 히트 74%가 중앙값 6영업일 내 눌림목 전환") | 구체 수치 | 근거 `docs/leader_to_pullback_watch.md`(`leader_check_cost.py`/`leader_to_pullback_dist.py`)가 README 감사표에서 **"❌ 재현 불가"** | **있음(추적 불가)** | **중간** — 관찰 등록 후 대기 워크플로의 근거 수치 | scripts/measurements/README.md:113 |
| GUIDE.md:476-483(부록, A<B<C<D 등급 역순 EV) | 등급별 구체 EV(0.140/0.196/0.202/0.385R) | 근거 `docs/ud_volume_ratio_investigation.md`가 README 감사표에서 **"❌ 재현 불가"** — 단 GUIDE 자체가 이미 "D는 n=26 표본부족" 캐비어트를 달아둠 | **있음(추적 불가, 과잉확신은 아님)** | **낮음~중간** | scripts/measurements/README.md:115 |
| GUIDE.md:293("돌파 종목의 절반은 며칠 내 피벗을 재테스트") | 구체 수치(절반, 며칠 내) | **직접 검증: 전후 어디에도 인용 없음**, 리포 내 대응 측정 스크립트 없음 | **있음(근거 없음)** | **중간** — 되돌림 대기(2차 진입) 전략의 전제 수치 | GUIDE.md:293 |
| GUIDE.md:263("오후 늦은 돌파는 실패율↑") | 정성적 주장 | **직접 검증: 인용 없음** | **있음(근거 없음)** | **낮음** — 최종 재확인 체크리스트 한 줄 | GUIDE.md:263 |
| GUIDE.md:97,118(FTD 성공률 60~70%, 오닐 "주도주 절반은 업종 힘") | 구체 수치 | 이 리포 자체 백테스트가 아니라 외부 CAN SLIM/오닐 방법론 서술로 보임(리포 내 대응 측정 없음) | 참고사항(이 리포 실측처럼 오독될 소지는 있으나 불일치로 단정 안 함) | **낮음** | GUIDE.md:97,118 |
| (검증, 문제없음) GUIDE.md:15 "손절폭≤ATR×1.5" | CLAUDE.md "ATR 상대 손절폭 v4.67" 인용 | 정확히 일치 | 없음 | - | - |
| (검증, 문제없음) GUIDE.md 철회 마크 문구들(RSI/A~D등급) | 이미 오늘 세션 초반에 반영된 최신 상태 | 정확히 일치, 정상 | 없음 | - | - |

**요약**: GUIDE.md 6.5장 자체 내부모순(위 표 1행)이 static/index.html의
종가베팅 z값 불일치와 **동일 원인**(v5.149에서 일부 문단만 갱신하고
나머지를 놓침) — 한 번의 실수가 두 파일에 걸쳐 총 6곳(static 5 + GUIDE
1)에 흩어져 있었던 셈. 두 번째로, "실측 기반"이라 단언하는 문구 중
상당수가 이 프로젝트 자신의 규칙(README rule1/4, "결정근거 스크립트는
커밋") 기준으로 이미 "재현 불가" 낙인이 찍힌 문서를 인용하고 있어
추적 가능성이 없다.

## 3. scanner.py/app.py 변수·필드 이름 vs 실제 의미

| 위치 | 이름 | 실제 의미 | 불일치 여부 | 심각도 | 근거 |
|---|---|---|---|---|---|
| scanner.py:1585 | `vol_high` | ATR%(변동성) >= 7.0 — "고변동 종목(하루 7%+ 변동)". **거래량(volume)과 무관** | **있음(직접 검증 완료)** — 같은 파일에서 `vol_` 접두사가 두 가지 의미로 충돌: `vol_dry`(1486/1914행)·`vol_ratio`(158,1485행)·`vol_up`(352행)은 전부 진짜 거래량인데 `vol_high`만 유일하게 ATR%(변동성)을 가리킴 — "거래량 높음"으로 오독하기 매우 쉬운 이름 | **높음** — `static/index.html:1197`(진입신호등 entrySignal 판정)에 직접 쓰임. app.py 자신의 주석(2205,3606행)은 정확히 이해하고 있어 로직 자체는 정상이지만 이름이 자기 파일 명명 관례와 충돌 | scanner.py:1585 vs 1486/1914(vol_dry), 158/1485(vol_ratio), 352(vol_up) — 전부 진짜 거래량 |
| app.py:5652 `warn_count` | "⚠️경보" 집계로 보이는 이름 | `alert`(사용자 등록 경보) OR `risk_warn`(과대리스크) 합산 — 확인 결과 static/index.html:2261이 이미 "⚠️ 경보/과대리스크"로 두 의미를 라벨에 명시 | 없음(검증 완료, 오탐 배제 기록) | - | static/index.html:2261 |
| app.py:10630 `stale`(포지션 동기화) | "오래됨" | `synced_at` 경과시간이 `POSITIONS_STALE_HOURS` 이상 — 이름 그대로 정확 | 없음(검증 완료, 정상) | - | app.py:10630-10634 |

*(CLAUDE.md에 이미 추적 중인 5건 — vol_high/atr_tight의 badge_fields
누락 갭, `_risk_hard_ok` pivot 기준 불일치, price_ago 클램프,
`to_code` 접미사 미검증, `/data` 저널 캐시 레이스 — 는 이미 알려진
갭이라 중복 보고 안 함, CLAUDE.md 먼저 읽고 대조함.)*

## 4. 문서 수치 vs CONFIG 실제 임계값

| 위치 | 문서상 수치 | CONFIG 실제 값 | 불일치 여부 | 심각도 | 근거 |
|---|---|---|---|---|---|
| **CLAUDE.md:83** | "rs_min이 80~85 사이인 경계 종목(imminent/breakout/boxbreak는 rs_min=85)" | **직접 검증**: `BREAKOUT_CONFIG["rs_min"]=75`(scanner.py:2504), `BOXBREAK_CONFIG["rs_min"]=75`(2662), `IMMINENT_CONFIG["rs_min"]=80`(2845) — 셋 다 85 아님 | **있음(직접 검증 완료)** — v5.61 당시엔 85였을 수 있으나 이후 v5.69/v5.70에서 탭별로 75/75/80으로 재조정된 뒤 이 문장이 안 고쳐짐 | **높음** — GUIDE.md가 아니라 **CLAUDE.md 자체**(모든 세션이 1차로 읽는 지침 파일)의 서술이라 파급력이 큼 | scanner.py:2504,2662,2845 vs CLAUDE.md:83 |
| GUIDE.md:15 "손절폭≤ATR×1.5", 완화상한 15% | `risk_hard_atr_mult=1.5`, `risk_hard_atr_cap=15.0`(scanner.py:804-805) | 일치 | 없음(검증완료, 정상) | - | - |
| GUIDE.md:238,362 "고정 US8%/KR12%" | `risk_hard_us=8.0`, `risk_hard_kr=12.0`(scanner.py:795-796) | 일치 | 없음(검증완료, 정상) | - | - |
| GUIDE.md:295 "돌파/박스돌파 연장상한 +12%" | `BREAKOUT_CONFIG["extended_max"]=0.12`, `BOXBREAK_CONFIG["extended_max"]=0.12`(scanner.py:2509,2672) | 일치 | 없음(검증완료, 정상) | - | - |
| KR 3억원/US $2M 유동성 하드컷 | GUIDE.md에 구체 숫자로 명시된 곳 없음(개념만 언급) | harness.py:246-247/app.py 3곳 실제값 3e8/2e6 | 비교 대상 부재(불일치 아님) | - | - |

---

## 종합 요약 (1~4번 전체)

**높음 심각도 6건 — 처리 상태**:
1. ✅ **v5.152~153에서 처리 완료** — static/index.html 5곳(종가베팅 카드/탭설명 z=4.28·+1.22%·n=276 하드코딩)을 `JONGGA_BACKTEST_NOTE` 참조 또는 숫자 제거+문서링크로 단일소스화.
2. ✅ **v5.153에서 처리 완료** — GUIDE.md 6.5장 "근거" 문단을 z=3.54/n=292/+0.80%로 직접 갱신(구수치는 참고용 병기).
3. ✅ **v5.152에서 처리 완료** — CLAUDE.md:83 "imminent/breakout/boxbreak rs_min=85"를 실제값(breakout/boxbreak=75, imminent=80)으로 정정.
4. ✅ **v5.152에서 처리 완료** — 포지션 탭 `RSI<50` 배지 제거.
5. ✅ **v5.154에서 처리 완료** — `vol_high`(scanner.py)를 `atr_pct_high`로 개명(6곳 한 커밋: scanner.py 2곳, static/index.html 1곳, app.py 주석 2곳, CLAUDE.md 1곳). 개명 후 grep 확인: 기능적 참조는 전부 새 이름, "vol_high" 잔존은 (a) 배포 무관 좀비 파일(루트 index.html), (b) 의도적 이력 표기("v5.154 이전 이름 vol_high")뿐 — 상세는 아래 절.
6. (참고) 1·2번은 같은 v5.149 편집 실수 하나가 두 파일 총 6곳에 흩어진 것이었음 — 근본원인은 1건, 재발 방지 절차를 CLAUDE.md에 신설(v5.153).

**중간 심각도 — 처리 순서·상태(2026-09-03, 사용자 지시 순서대로)**:
1. ✅ **v5.159에서 처리 완료** — 저널 "승률" 두 정의 공존. `journalStats()`
   (손절가 기록된 종료건만 분모, R배수 양수 비율)를 "승률(R+)"로,
   `closedTabStats()`(청산가만 있으면 분모 포함, 원시등락률 양수 비율)를
   "승률(청산가+)"로 라벨 분리 + 툴팁 + 표본크기(n) 병기. 코드 확인상
   진짜 차이는 부호가 아니라 분모(static/index.html 5991행: 손절가 없으면
   result_r을 아예 안 채움) — 실제 갭 건수는 프로덕션 데이터 접근 불가로
   미확인, 배포 후 화면의 두 n값 비교로 사용자가 직접 확인 가능.
2. ⏳ 미처리 — GUIDE "재현 불가" 문서 인용(슈퍼대장 활용법·대장전환74%).
3. ⏳ 미처리 — GUIDE 근거 없는 문구 2건(돌파 절반 재테스트, 오후 늦은 돌파).
4. ⏳ 미처리 — 재점화 "대기 N개" 헤더 과대표현(확인진입 포함).
5. ⏳ 미처리 — entrySignal 주석("모든 탭 반영" vs 실제 눌림목 전용).

**함께 처리(2026-09-03, v5.159에서 같이 반영)**: 일지 탭 상단에 안내
문구 "신호 추적 기록 — 실제 체결 아님, 계좌 성과는 포지션 탭 참조"
추가 — 일지가 신호 검증용이라 미체결(관찰만) 건도 담는데 통계가 계좌
성과처럼 읽히는 문제 대응. 데이터 구조 무변경, 문구만 추가.

**낮음 심각도(미처리, 목록 유지)**: entrySignal/vol_high 코드 주석류, GUIDE A~D등급 수치(이미 캐비어트 있음), "오후 늦은 돌파" 등 무근거 정성적 문구.

**검증 완료·문제없음(오탐 배제 기록)**: stage2 tier 임계값, theme_lifecycle 3일 하락 로직, money_flow "확산(본격)" 판정, `warn_count`/`stale` 필드, ATR배수·리스크하드게이트·연장상한 CONFIG값, 캘린더 "오늘의 결정" 티어 로직.

## 4순위 파급범위 조사 — `vol_high` → `atr_pct_high` 개명

**✅ 조사 완료 후 승인받아 v5.154에서 개명 완료.**

**정의/할당 지점(2곳, 전부 scanner.py `analyze()` 내부, 눌림목 탭 전용)**:
- `scanner.py:1585` — 지역변수 `vol_high = atr_pct >= 7.0`
- `scanner.py:1653` — 반환 dict 키 `"vol_high": vol_high` (여기서 `/api/scan` JSON 응답 계약의 일부가 됨)

**참조/소비 지점 — 전수 확인, 딱 1곳**:
- `static/index.html:1197` — `entrySignal()`의 `if (s.atr_pct != null && s.vol_high) { if (s.atr_tight) {...} }` — **`vol_high` 자체는 절대 화면에 라벨로 안 뜬다.** `atr_tight`가 참일 때만 실제 경고문("변동성 ATR X% — 손절 대비 큼")이 뜨는데, `vol_high`는 그 경고를 띄울지 말지 결정하는 **1차 게이트**(고변동 종목일 때만 이 체크를 아예 켬) 역할만 한다.
- `entrySignal` 외 다른 용도(필터·정렬·배지 텍스트 등) 전무 — grep 전수 확인.

**`vol_high`라는 이름을 언급만 하는 곳(개명 시 같이 안 고치면 새로 어긋나는 지점)**:
- `app.py:2230`, `app.py:3631` — changelog 주석 2곳(비표시, 낮음 위험).
- **`CLAUDE.md:86`**(알려진 설계 갭 항목) — `vol_high`/`atr_tight`를 이름으로 직접 언급. **개명하면서 이 줄을 안 고치면, 이번에 만든 안전장치 문서 자체가 다음 라벨-의미 불일치 사례가 되는 아이러니가 생긴다 — 반드시 같은 커밋에 포함.**
- `docs/label_semantics_audit_2026-09-02.md`(이 문서) — 개명 완료 시 이 절도 갱신 필요.

**개명 불필요/무관 확인**:
- 테스트 파일(`test_trace_*.py`) 전수 grep — `vol_high` 참조 없음, 테스트 파손 위험 없음.
- `/api/debug/{ticker}` 등 진단 엔드포인트가 이 필드를 별도로 특별 취급하는 코드 없음(analyze() 반환 dict가 그대로 통과할 뿐).
- 봇(`/api/ma/{ticker}`) 등 외부 연동이 이 필드를 쓴다는 근거 없음.
- 루트의 좀비 `index.html`(CLAUDE.md 규칙1, 배포 안 됨)과 `.claude/worktrees/woolly-wondering-gray/`(과거 격리 작업용 git worktree 잔재로 추정, 배포 무관)에도 동일 코드 사본이 있으나 **둘 다 라이브 서비스와 무관 — 개명 시 안 고쳐도 실제 영향 없음**(다만 안 쓰는 파일이 계속 남아있다는 것 자체는 이번 조사와 별개 사안).

**개명 시 위험 지점 요약**:
1. **JSON 키 이름 자체를 바꾸면**(`"vol_high"` → `"atr_pct_high"`) `/api/scan` 응답 계약이 바뀐다 — 유일한 소비처(static/index.html:1197)를 **같은 커밋에서 동시에** 안 고치면, 에러 없이 `s.atr_pct_high`가 `undefined`가 되어 게이트가 항상 거짓으로 평가되고 **눌림목 탭 전체의 ATR 변동성 경고 체크가 조용히 완전히 꺼진다**(콘솔 에러도 안 남는 조용한 회귀 — 가장 조심해야 할 지점).
2. CLAUDE.md:86을 같이 안 고치면 그 줄 자체가 새로운 라벨-의미 불일치가 됨(위에서 설명).
3. 그 외 위험은 낮음 — 테스트·외부연동·다른 탭 전무 확인.

**결론**: 소비처가 정확히 1곳뿐이고 테스트/외부계약 영향이 없어 **개명 자체의 기술적 위험은 낮다.** 다만 scanner.py(2곳)+static/index.html(1곳)+CLAUDE.md(1곳)+app.py 주석(2곳) 총 6곳을 **한 커밋에서 동시에** 바꿔야 안전 — 부분 반영 시 위 1번 회귀가 발생한다.

**개명 완료 후 검증(v5.154)**: `grep -rn "vol_high" --include="*.py" --include="*.html" --include="*.md" .`
실행 결과, 기능적 참조(scanner.py 정의·반환, static/index.html 소비,
`/api/scan` JSON 키)는 전부 `atr_pct_high`로 전환 완료. 남은
"vol_high" 문자열은 두 종류뿐: ① **배포와 무관한 좀비 파일**(루트
`index.html:561` — CLAUDE.md 규칙1, 아무도 안 봄) ② **의도적 이력
breadcrumb**(app.py 주석 2곳·CLAUDE.md 1곳에 "v5.154 이전 이름
vol_high"라고 명시적으로 남긴 것 — 다음에 이 필드를 찾는 사람이
구 이름으로 검색해도 새 이름을 찾을 수 있게 하는 의도적 장치이지
잔존 버그가 아님). 이 감사 문서 자체도 "vol_high"를 다수 언급하지만
투자 대상 결함을 그대로 기록한 조사 기록이라 당연히 남겨둠. **기능
코드 기준으로는 잔존 0건.**

